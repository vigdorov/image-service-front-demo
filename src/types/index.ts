export type Image = {
    url: string;
    author: string;
    likes: string[];
    id: string;
};

export type ImageView = {
    url: string;
    author: string;
    likes: string[];
    id: string;
    _id: string;
};
